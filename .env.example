# Twilio credentials
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_FROM_NUMBER=+19206968892
BASE_URL=https://your-ngrok-or-server-url.com
TWILIO_LANGUAGE=zh-HK

# Voice Assistant Configuration
FIRST_MESSAGE=你好，我係祖兒呀，請問點稱呼你呀？
SYSTEM_PROMPT=你係一個用廣東話嘅 AI 陪伴照護員，你的名字叫祖兒。如果 User memory 有用戶名字就用佢嘅名字稱呼佢；如果唔知名字，對話開始時先有禮貌地問佢點稱呼，然後一直用佢嘅名字。你係語音通話，唔係文字。

# Azure Speech-to-Text
AZURE_SPEECH_KEY=your_azure_speech_key
AZURE_SPEECH_REGION=eastasia
AZURE_SPEECH_LANGUAGE=zh-HK

# OpenClaw LLM (primary, HTTP)
# Use internal Zeabur address for lower latency when both services are on Zeabur:
# OPENCLAW_URL=http://openclaw.zeabur.internal:18789
OPENCLAW_URL=https://voiceclaw.zeabur.app
OPENCLAW_TOKEN=your_openclaw_token

# Google Gemini — set USE_GEMINI_DIRECT=true to bypass OpenClaw for all queries
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.5-flash-lite
# USE_GEMINI_DIRECT=true

# Business Console callback — POST call transcript + outcome after each outbound call
CONSOLE_CALLBACK_URL=https://your-business-console.zeabur.app

# LLM Configuration (OpenRouter fallback)
LLM_BASE_URL=https://openrouter.ai/api/v1/chat/completions
LLM_API_KEY=your_openrouter_api_key
LLM_MODEL=deepseek/deepseek-chat:free

# MiniMax Text-to-Speech
MINIMAX_API_KEY=your_minimax_api_key
MINIMAX_MODEL=speech-02-turbo
MINIMAX_VOICE_ID=Cantonese_GentleLady
MINIMAX_LANGUAGE_BOOST=Chinese,Yue
# Optional: path to a PCM16 16kHz mono WAV or raw file to mix as background ambient sound
# AMBIENT_AUDIO_FILE=/path/to/office-ambient.wav
# AMBIENT_VOLUME=0.15

# Redis
REDIS_CONNECTION_STRING=redis://localhost:6379

# Qdrant vector DB
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=knowledge_base

# Embedding service (used by Qdrant for semantic search)
EMBEDDING_API_URL=https://your-openai-endpoint/v1/embeddings
EMBEDDING_API_KEY=your_embedding_api_key
EMBEDDING_MODEL=text-embedding-ada-002

# Media Streams (streaming architecture — set to 'true' to enable, 'false' for legacy record/webhook)
USE_MEDIA_STREAMS=false

# Azure TTS voice for streaming path (streaming uses Azure TTS, legacy uses MiniMax)
AZURE_TTS_VOICE=zh-HK-HiuGaaiNeural

# Server
PORT=3000

# ── CTM Services ─────────────────────────────────────────────────────────────

# CTM LLM (OpenAI-compatible Qwen)
USE_CTM_LLM=false
CTM_LLM_BASE_URL=http://125.31.4.218:3001/v1
CTM_LLM_API_KEY=
CTM_LLM_MODEL=Qwen

# CTM TTS
USE_CTM_TTS=false
CTM_TTS_URL=http://<server_ip>:80
CTM_TTS_VOICE=pakchi
CTM_TTS_MODEL=ctm_tts
CTM_TTS_SAMPLE_RATE=16000

# CTM ASR (WebSocket)
USE_CTM_STT=false
CTM_ASR_URL=wss://aiapi.ctmcloud.com.mo/Others/websocket-uat2/ws
CTM_ASR_ACCESS_CODE=
CTM_ASR_LANGUAGE=cantonese

# Per-hotline provider overrides — business console passes these as URL params to /stream-fs
# Values: auto | ctm | minimax | azure | gemini | groq | openrouter | openclaw
LLM_PROVIDER=auto
TTS_PROVIDER=auto
STT_PROVIDER=auto
